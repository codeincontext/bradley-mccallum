{
  "Main": {
    "title": {
      "type": "StructuredText",
      "config": {
        "single": "heading1",
        "label": "Title"
      }
    },
    "uid": {
      "type": "UID",
      "config": {
        "label": "URL"
      }
    },
    "venue": {
      "type": "Text",
      "config": {
        "label": "Venue"
      }
    },
    "location": {
      "type": "Text",
      "config": {
        "label": "Location"
      }
    },
    "date": {
      "type": "Date",
      "config": {
        "label": "Date"
      }
    },
    "curator": {
      "type": "Text",
      "config": {
        "label": "Curator"
      }
    },
    "exhibition_type": {
      "type": "Select",
      "config": {
        "options": ["group"],
        "label": "Solo or group",
        "placeholder": "solo"
      }
    },
    "project": {
      "type": "Link",
      "config": {
        "select": "document",
        "customtypes": ["project"],
        "label": "Featured in project (will create a link)"
      }
    },
    "body": {
      "type": "Slices",
      "fieldset": "Body",
      "config": {
        "choices": "%%SLICES%%"
      }
    }
  }
}
